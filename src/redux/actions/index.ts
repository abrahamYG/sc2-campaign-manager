import {addCampaign, selectCampaign, setCampaigns, setCampaignsLocal} from "./campaign"
import {getConfig,setConfig} from './config'
let nextTodoId = 0;

export {addCampaign, selectCampaign,setCampaigns, setCampaignsLocal, getConfig, setConfig};

//export const setFilter = filter => ({ type: SET_FILTER, payload: { filter } });
